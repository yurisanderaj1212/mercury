{
  "name": "@mercury/scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "Mercury scheduled tasks — publication expiry, market stats refresh, alert evaluation",
  "scripts": {
    "expire-publications": "TASK=expire-publications ts-node src/scheduler.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "prisma": "^6.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
