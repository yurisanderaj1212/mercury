{
  "name": "@mercury/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and domain interfaces for Mercury",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
